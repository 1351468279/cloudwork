name: cloudwork_app
description: "CloudWork Mobile AI Coding Agent Remote Commander"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.2
  qr_code_scanner: ^1.0.1
  web_socket_channel: ^3.0.1
  cryptography: ^2.7.0
  provider: ^6.1.2
  flutter_syntax_view: ^0.0.1
  google_fonts: ^6.2.1
  uuid: ^4.5.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
